import styled from 'styled-components';

const StyledLoader = styled.div`
  grid-column: 2 / end;
  grid-row: 1 / end;

  font-size: 3rem;
  display: grid;
  place-items: center;
`;
export default StyledLoader;
